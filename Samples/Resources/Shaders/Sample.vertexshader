#version 330 core

uniform mat4 proj;
uniform mat4 mv;
uniform	vec3 eye;		// eye position

layout(location = 3)in  vec3 posSize;		// position in xyz, size in w

void main(void)
{
    gl_Position  = proj * mv * vec4 ( posSize, 1.0 );
}